# Searching-Techniques
Searching techniques in DSA are used to find elements in a dataset efficiently. Linear search checks elements sequentially and works on any list (O(n)). Binary search works on sorted data by dividing the search space, making it faster (O(log n)).
